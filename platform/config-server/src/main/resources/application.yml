spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: ${CONFIG_REPO_PATH:file:./config-repo,file:../config-repo,file:../../config-repo}

server:
  address: 0.0.0.0
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
